{{!-- Fire tier description (comfort + encounter in one line; color spans only) --}}
{{#if (eq id "embers")}}{{{localize 'IONRIFT.RESPITE.FIRE.EmbersBody'}}}{{else if (eq id "campfire")}}{{{localize 'IONRIFT.RESPITE.FIRE.CampfireBody'}}}{{else if (eq id "bonfire")}}{{{localize 'IONRIFT.RESPITE.FIRE.BonfireBody'}}}{{/if}}
